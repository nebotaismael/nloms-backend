setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
  config:
    NODE_ENV: production
    NPM_CONFIG_PRODUCTION: true

build:
  docker:
    web: Dockerfile

release:
  command:
    - psql $DATABASE_URL < migrations/init.sql
  image: web

run:
  web: node server.js